{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://citadel.contracts/schemas/story/scene.schema.json",
  "title": "Scene",
  "description": "A playable scene. ADOPTED from the R3 candidate draft (citadel-planning/10-content-candidates/chapter-01-bundle-v0.1) at sha256 fb177ccb0d9caaf7354d1e7e826c12a0998176b1aeef0bbf448380d86c488b53. That folder is now INPUT, not authority: this file is the schema. R3 B1.",
  "type": "object",
  "required": [
    "id",
    "chapter",
    "locations",
    "present",
    "play_mode",
    "orientation",
    "desire",
    "friction",
    "choice_or_discovery",
    "turn",
    "residue",
    "begins_after",
    "ends_before",
    "tension_axes",
    "role_variants",
    "creative_tests",
    "text_equivalent",
    "skippable",
    "keyboard_reachable",
    "required_reveals",
    "real_world_bridge"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^sc-[0-9]{2}-[0-9]{2}$"
    },
    "chapter": {
      "type": "string",
      "pattern": "^ch-[0-9]{2}$"
    },
    "quests": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "locations": {
      "description": "CORRECTION 1. Canon Scene 1 spans the Gate of Names, the Clinical Sorting Court and the emergency courts; Scene 2 spans three. A single location cannot express it.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    },
    "present": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "play_mode": {
      "enum": [
        "solo",
        "small-team",
        "facilitated",
        "either"
      ]
    },
    "orientation": {
      "type": "object",
      "required": [
        "key",
        "what_matters_now"
      ],
      "properties": {
        "key": {
          "type": "string"
        },
        "what_matters_now": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "desire": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "character_id",
          "wants"
        ],
        "properties": {
          "character_id": {
            "type": "string"
          },
          "wants": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    "friction": {
      "type": "string",
      "minLength": 1
    },
    "choice_or_discovery": {
      "description": "A scene with only orientation and no choice_or_discovery is the 'scene that only explains' failure, and is rejected.",
      "type": "string",
      "minLength": 1
    },
    "turn": {
      "type": "string",
      "minLength": 1
    },
    "residue": {
      "type": "string",
      "minLength": 1
    },
    "in_world_deadline": {
      "description": "Dramatic and in-world. NEVER a real-time countdown, and never a scoring input (DEC-014).",
      "type": "object",
      "properties": {
        "closes_at": {
          "type": [
            "string",
            "null"
          ]
        },
        "consequence_if_missed": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "begins_after": {
      "type": "string",
      "minLength": 1
    },
    "ends_before": {
      "type": "string",
      "minLength": 1
    },
    "tension_axes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "axis",
          "severity"
        ],
        "properties": {
          "axis": {
            "enum": [
              "time",
              "information",
              "authority",
              "resource",
              "trust"
            ]
          },
          "severity": {
            "enum": [
              "light",
              "moderate",
              "hard"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "role_variants": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "role_id",
          "starting_position",
          "information_held",
          "options_available"
        ],
        "properties": {
          "role_id": {
            "type": "string"
          },
          "starting_position": {
            "type": "string"
          },
          "information_held": {
            "type": "string"
          },
          "options_available": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      }
    },
    "required_reveals": {
      "description": "CORRECTION 4. Canon guarantees the clue REGARDLESS of role. Every entry must be reachable on every selectable role path; reachable_on_all_role_paths records that this was checked, and the validator refuses false.",
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id",
          "what",
          "reachable_on_all_role_paths"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "what": {
            "type": "string",
            "minLength": 1
          },
          "reachable_on_all_role_paths": {
            "const": true
          },
          "how_each_role_reaches_it": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "role_id",
                "route"
              ],
              "properties": {
                "role_id": {
                  "type": "string"
                },
                "route": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "evidence_ids": {
            "type": "array",
            "uniqueItems": true,
            "items": {
              "type": "string",
              "pattern": "^ev\\."
            },
            "description": "EVS-4. Which evidence discharges this reveal. BY ID: the guarantee that a clue cannot disappear because of role selection is only checkable if the link is a reference rather than a resemblance."
          }
        },
        "additionalProperties": false
      }
    },
    "real_world_bridge": {
      "description": "CORRECTION 3. The Chapters 1-3 prohibition is enforced by the SCHEMA, not by prose or a grep: a canon-violating scene is unloadable. Canon states 'No formal assessment or transfer artifact is completed in Chapters 1-3.' For ch-01..ch-03, kind MUST be observation_record or none. 'artifact' is not an accepted value anywhere in this draft.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "observation_record",
            "none"
          ]
        },
        "id": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "gate": {
      "type": "object",
      "properties": {
        "requires": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "variable",
              "min_band"
            ],
            "properties": {
              "variable": {
                "$ref": "#/$defs/seasonVariable"
              },
              "min_band": {
                "$ref": "#/$defs/band"
              }
            },
            "additionalProperties": false
          }
        },
        "on_fail": {
          "type": "object",
          "required": [
            "substitute",
            "absence_is_noticed"
          ],
          "properties": {
            "substitute": {
              "type": [
                "string",
                "null"
              ]
            },
            "absence_is_noticed": {
              "type": "boolean"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "creative_tests": {
      "description": "At least three must be true.",
      "type": "object",
      "required": [
        "someone_wants_something_now",
        "legitimate_need_in_tension",
        "reveals_character_through_action",
        "exposes_operational_dependency",
        "player_decides_or_influences",
        "consequence_can_return",
        "advances_mystery",
        "transfers_to_real_hospital"
      ],
      "properties": {
        "someone_wants_something_now": {
          "type": "boolean"
        },
        "legitimate_need_in_tension": {
          "type": "boolean"
        },
        "reveals_character_through_action": {
          "type": "boolean"
        },
        "exposes_operational_dependency": {
          "type": "boolean"
        },
        "player_decides_or_influences": {
          "type": "boolean"
        },
        "consequence_can_return": {
          "type": "boolean"
        },
        "advances_mystery": {
          "type": "boolean"
        },
        "transfers_to_real_hospital": {
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "text_equivalent": {
      "type": "object",
      "required": [
        "key"
      ],
      "properties": {
        "key": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "skippable": {
      "const": true
    },
    "static_fallback": {
      "type": [
        "string",
        "null"
      ]
    },
    "keyboard_reachable": {
      "const": true
    },
    "asset_slots": {
      "description": "Slot ids only, never file paths. A null or absent slot appears in the computed asset manifest as an unfilled slot, which is the point: the manifest is derived, never hand-maintained.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^slot\\."
      }
    },
    "unresolved": {
      "description": "Where canon is silent, record it here rather than inventing a default. 'No invented defaults' is the same rule the content baseline applies to clinical and engineering thresholds.",
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "field",
          "why"
        ],
        "properties": {
          "field": {
            "type": "string"
          },
          "why": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    "emotional_state": {
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "additionalProperties": false,
      "required": [
        "register",
        "derivedFrom"
      ],
      "description": "V9. The scene's emotional register, which drives how it opens.\n\n⚠️ CANON ASSIGNS THE ARC PER CHAPTER, NOT PER SCENE. Chapter 1's is 'wonder -> professional belonging -> concentrated fear -> first unease' (one line, at chapter level). Distributing it across scenes is a DERIVATION, so `derivedFrom` carries the canon term the register came from and the two are never conflated.\n\n⚠️ AND THE VOCABULARIES DO NOT ALIGN. art-direction-and-asset-model.md section 2 defines seven registers with visual meanings; canon's 'concentrated fear' is not one of them. The register enum below is the art model's, because those are the terms with a defined visual register — a state with no defined register cannot drive anything.",
      "properties": {
        "register": {
          "enum": [
            "wonder",
            "belonging",
            "unease",
            "pressure",
            "loss",
            "recognition",
            "earned-hope"
          ],
          "description": "From art-direction-and-asset-model.md section 2. NOTE: that section says 'ten emotional states' and tabulates SEVEN. The three unlisted states have no defined visual register, so they are not enum members here — a member with no register would be a state nothing could render."
        },
        "derivedFrom": {
          "type": "string",
          "minLength": 1,
          "description": "The canon term this register was derived from, verbatim. Required so a reviewer can see the interpretation rather than inherit it."
        },
        "unresolved": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Set when canon does not name a register for this scene, saying what is unresolved. Canon is read-only; where it is silent that silence is recorded, never filled in."
        }
      }
    },
    "staging": {
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "additionalProperties": false,
      "required": [
        "pre_commit",
        "interactive",
        "post_commit",
        "scene_exit"
      ],
      "description": "EVS-1. WHEN each authored movement is presented. The six movements are SOURCE MATERIAL, not six headings to display before the player acts (Final Product Experience Contract section 2).\n\nFPE-01 is the invariant this field exists to make enforceable: `turn`, the immediate effect, the state delta and `residue` are not rendered before commitment. Without staging there is nothing for a runtime to obey — the current renderer emits all six movements as one ordered document, so the interface spoils its own drama and no rule can object.\n\nWHY PER SCENE AND NOT A CONSTANT. Today all four Chapter 1 scenes stage identically, and identical data in four places is a place for drift. It is per-scene anyway because the `interactive` phase varies per scene the moment EVS-4 adds inspect/compare and consult/coordinate actions — and because a mapping that can be WRONG is a mapping the schema can refuse. A constant would make the refusal tests below unwritable.\n\nTHE PHASE SEQUENCE ITSELF IS NOT DATA. pre_commit -> interactive -> post_commit -> scene_exit is fixed by this object's four required keys, so a scene cannot declare its phases out of order. What a scene CAN get wrong is which movement sits in which phase, and each phase's `items` enum is what refuses that.",
      "properties": {
        "pre_commit": {
          "description": "Arrive and encounter. What the player may see BEFORE they act. `turn` and `residue` are absent from this enum on purpose — that absence is FPE-01.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "orientation",
              "desire",
              "friction"
            ]
          }
        },
        "interactive": {
          "description": "Investigate, coordinate, commit. EVS-1 shipped one member and said why: 'members are not declared before the action they name exists — an enum member nothing can produce is a rule that cannot fire.' EVS-4 built the actions, so `actions` joins it here.\n\n`actions` stages the scene's inspect and consult controls. `choice_or_discovery` stages the commitment. A scene may present both, and the order in the array is the order they appear — investigation before commitment is the point.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "actions",
              "choice_or_discovery"
            ]
          }
        },
        "post_commit": {
          "description": "The response beat. FPE-02: a commitment receives an immediate causal response BEFORE navigation advances. `contains` makes `immediate_effect` mandatory here, so a scene cannot stage a response beat with nothing in it.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "contains": {
            "const": "immediate_effect"
          },
          "items": {
            "enum": [
              "turn",
              "immediate_effect"
            ]
          }
        },
        "scene_exit": {
          "description": "What is left behind. `residue` is the only member, so residue staged anywhere else is refused by this enum and residue staged here is the only accepted form.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "residue"
            ]
          }
        }
      }
    },
    "immediate_effect": {
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "additionalProperties": false,
      "required": [
        "state_change_source",
        "responses"
      ],
      "description": "EVS-1. The response beat's material, per committed option.\n\n★ THE STATE CHANGE IS NOT RESTATED HERE. It is the chosen option's own typed `effects` with `visible: true`, which decision.schema.json already governs. Copying them into the scene would be one shape defined twice — the defect `generate-types.js` exists to prevent, one layer up.\n\n★ AND THE PROSE IS NOT INVENTED HERE. Chapter 1 canon authors, per pathway, the operational consequence and the state change; it authors no post-commitment narration. So `narrative_response` is null wherever canon is silent, and `derived_from` names the authored material the runtime composes the beat from instead. A null response with nothing to derive from would be an empty beat wearing a required field's name, which is why null requires both `derived_from` and `unresolved`.\n\n⚠️ UNRESOLVED, DELIBERATELY: a discovery-only scene. `responses` is keyed by `option_id`, so a scene whose `choice_or_discovery` is prose rather than a decision reference cannot be represented. All four Chapter 1 scenes carry decisions, so nothing is blocked; a discovery shape is not invented here because EVS-4 is where discovery actions get defined and inventing one now would fix the wrong shape first.",
      "properties": {
        "state_change_source": {
          "const": "decision_effects",
          "description": "Where the player-observable state change comes from. One accepted value, because there is one authority: the decision option's typed effects."
        },
        "responses": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "option_id",
              "narrative_response",
              "character_response"
            ],
            "properties": {
              "option_id": {
                "type": "string",
                "minLength": 1
              },
              "narrative_response": {
                "type": [
                  "object",
                  "null"
                ],
                "additionalProperties": false,
                "required": [
                  "key"
                ],
                "properties": {
                  "key": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "description": "Authored post-commitment prose, as a locale key. Null where canon authors none."
              },
              "character_response": {
                "type": [
                  "array",
                  "null"
                ],
                "minItems": 1,
                "description": "Who reacts, and how. AN ARRAY, because canon reacts in more than one voice: the Chapter 1 closure characterization assigns Fadl, Maha AND Rami a different action per pathway, and a single-character shape would have forced two of the three to be dropped. Null where canon is silent -- never filled in.",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "character_id",
                    "responds"
                  ],
                  "properties": {
                    "character_id": {
                      "type": "string",
                      "minLength": 1
                    },
                    "responds": {
                      "type": "string",
                      "minLength": 1
                    }
                  }
                }
              },
              "derived_from": {
                "type": "array",
                "uniqueItems": true,
                "items": {
                  "enum": [
                    "protects",
                    "risks",
                    "effects"
                  ]
                },
                "description": "Which authored material the runtime composes the response from when `narrative_response` is null. FPE-03 requires the response to name both the protected value and the cost, and `protects` + `risks` are exactly those, already authored on the option."
              },
              "unresolved": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "field",
                    "why"
                  ],
                  "properties": {
                    "field": {
                      "type": "string"
                    },
                    "why": {
                      "type": "string",
                      "minLength": 1
                    }
                  }
                },
                "description": "Same convention as the scene's own `unresolved`: where canon is silent, say so rather than invent a default."
              }
            },
            "allOf": [
              {
                "description": "A null narrative response must have something to derive the beat from, and must say what is owed. Otherwise \"no prose authored\" and \"no response beat at all\" are the same document.",
                "if": {
                  "properties": {
                    "narrative_response": {
                      "type": "null"
                    }
                  },
                  "required": [
                    "narrative_response"
                  ]
                },
                "then": {
                  "required": [
                    "derived_from",
                    "unresolved"
                  ],
                  "properties": {
                    "derived_from": {
                      "minItems": 1
                    },
                    "unresolved": {
                      "minItems": 1
                    }
                  }
                }
              }
            ]
          }
        }
      }
    },
    "bell": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "enum": [
        "first",
        "first_quarter",
        "second",
        "third",
        null
      ],
      "description": "V7. WHERE THE SCENE SITS IN THE DAY, IN THE WORLD’S OWN CLOCK. Canon’s Chapter 1 contract gives the played duration as “First Bell to shortly after the Third Bell”, and each scene setup names its bell. Position told as “Scene 2 of 4” would be true of any content; this is true of this world.\n\n⚠ THIS FIELD SHIPPED IN CONTENT BEFORE IT EXISTED HERE. `citadel` PR #25 added `bell` to all four Chapter 1 scenes; this schema is `additionalProperties: false`, so every one of those scenes was INVALID against the contract it pinned — and nothing found out, because citadel validated no content against the pinned schemas at all. EVS-1 added that validation and it failed on the first run. The gap was the check, not the field.\n\nTHE ENUM IS CHAPTER 1’S BELLS. Each member has a locale string; a bell with no string renders as its own key at the reader. A chapter naming a fourth bell extends this enum and adds its string in the same change — which is the point of an enum rather than a free string."
    },
    "evidence": {
      "type": [
        "array",
        "null"
      ],
      "default": null,
      "description": "EVS-4. WHAT CAN BE LEARNED, AND FROM WHOM.\n\n★ EVIDENCE IS PARTIAL AND IT HAS A SOURCE. Canon's Chapter 1 is built on two people reading accurate information in different rooms and reaching different conclusions — the Hall sees 'backup generation active', the far bay sees no supply. An engine that holds one true world state cannot express that; an engine that holds WHO SAID WHAT can.\n\nA scene declares its evidence here and its ACTIONS reveal it. Nothing is discovered by arriving.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "what",
          "source",
          "derivedFrom"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^ev\\."
          },
          "what": {
            "type": "string",
            "minLength": 1
          },
          "source": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "kind",
              "id"
            ],
            "description": "WHO OR WHAT IS SAYING THIS. Provenance travels with the fact, so a later debrief can ask where a belief came from rather than treating every held fact as equally sound.",
            "properties": {
              "kind": {
                "enum": [
                  "person",
                  "instrument",
                  "place",
                  "record"
                ]
              },
              "id": {
                "type": "string",
                "minLength": 1
              }
            }
          },
          "partial": {
            "type": "boolean",
            "default": true,
            "description": "Whether this fact is incomplete on its own. Canon: the map is accurate and stops before the chamber; the Hall reading is true and is not the whole truth. `false` marks a fact that stands alone."
          },
          "satisfies_reveal": {
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "description": "The required_reveal this evidence discharges, if any. WIRED BY ID, NEVER MATCHED ON PROSE — canon guarantees the clue regardless of role, and a guarantee checked by string comparison is not checked."
          },
          "derivedFrom": {
            "type": "string",
            "minLength": 1,
            "description": "The canon passage this was transcribed from. Required, so a reviewer can see what is canon and what is interpretation."
          }
        }
      }
    },
    "actions": {
      "type": [
        "array",
        "null"
      ],
      "default": null,
      "description": "EVS-4. WHAT THE PARTICIPANT CAN DO BEFORE COMMITTING.\n\n★ CANON ASKED FOR BOTH OF THESE BY NAME.\n\nInspection: 'place detailed timings in OPTIONAL INSPECTION or the later review rather than long crisis dialogue.'\n\nConsultation: 'The selected role supplies one direct authority. The player must SEEK OTHER JUDGMENTS from named clinical, nursing, operational, safety, information, and city partners.'\n\nSo the two action types are not a mechanic invented for a games checklist; they are how canon says the chapter is played. The third — commit — is `choice_or_discovery`, which already existed.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "type",
          "target",
          "reveals",
          "derivedFrom"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "type": {
            "enum": [
              "inspect",
              "consult"
            ],
            "description": "INSPECT reads a place, instrument or record. CONSULT asks a person, and a person may answer, qualify, refuse or withhold. The distinction is not cosmetic: only a person can decline."
          },
          "target": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "kind",
              "id"
            ],
            "properties": {
              "kind": {
                "enum": [
                  "person",
                  "instrument",
                  "place",
                  "record"
                ]
              },
              "id": {
                "type": "string",
                "minLength": 1
              }
            }
          },
          "reveals": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "pattern": "^ev\\."
            },
            "description": "Evidence ids from this scene. An action that reveals nothing is a button, and a button that does nothing teaches that the buttons do nothing."
          },
          "visible_to_roles": {
            "type": [
              "array",
              "null"
            ],
            "default": null,
            "items": {
              "type": "string"
            },
            "description": "Which roles may take this action. Null means every role. ⚠️ ROLE FILTERING MUST NOT BREAK THE GUARANTEE: canon states a required clue 'cannot disappear because of role selection', so a reveal must stay reachable through SOME action for every selectable role. That is a cross-document rule and lives in the consumer, not here."
          },
          "requires": {
            "type": "array",
            "default": [],
            "uniqueItems": true,
            "items": {
              "type": "string",
              "pattern": "^ev\\."
            },
            "description": "Evidence that must already be held for this action to be available. Used sparingly: canon narrates one comparison that genuinely follows from two prior sightings, and manufacturing further chains would be inventing structure."
          },
          "response": {
            "type": [
              "object",
              "null"
            ],
            "default": null,
            "additionalProperties": false,
            "required": [
              "character_id",
              "says"
            ],
            "description": "★ WHAT THE PERSON DOES, IN THEIR OWN POSITION. Canon's fail-forward is explicit: 'the professional owner states the binding limit and acts within existing authority.' `withholds` is that limit — what this person will NOT do, even when asked. A consult with no limit is a vending machine with a face on it.",
            "properties": {
              "character_id": {
                "type": "string",
                "minLength": 1
              },
              "says": {
                "type": "string",
                "minLength": 1
              },
              "withholds": {
                "type": [
                  "string",
                  "null"
                ],
                "default": null
              },
              "acts_independently": {
                "type": "boolean",
                "default": false,
                "description": "True where canon has the character act under their own authority whether or not the player asked — Rami isolating the board is the shipped case."
              }
            }
          },
          "cost": {
            "type": [
              "object",
              "null"
            ],
            "default": null,
            "additionalProperties": false,
            "required": [
              "currency",
              "what"
            ],
            "description": "⚠️ A DECLARED NOTE, NOT A QUANTITY. Canon names the currencies — 'a cost in time, trust, workload, service capacity, or evidence' — and attaches them to unsafe proposals and the fail-forward. It sets no prices. A number invented here would be a score wearing a lore costume, which DEC-005 exists to prevent, so this is rendered and recorded and never summed.",
            "properties": {
              "currency": {
                "enum": [
                  "time",
                  "trust",
                  "workload",
                  "service_capacity",
                  "evidence"
                ]
              },
              "what": {
                "type": "string",
                "minLength": 1
              }
            }
          },
          "derivedFrom": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  },
  "$defs": {
    "seasonVariable": {
      "description": "The eleven engine variables, which check-plan.sh asserts map 1:1 to canon's eleven persistent consequence threads. An invented variable name fails the plan mechanically.",
      "enum": [
        "V1",
        "V2",
        "V3",
        "V4",
        "V5",
        "V6",
        "V7",
        "V8",
        "V9",
        "V10",
        "V11"
      ]
    },
    "band": {
      "enum": [
        "critical",
        "low",
        "fragile",
        "adequate",
        "strong"
      ]
    },
    "effect": {
      "description": "CORRECTION 2. Typed operations, not direction plus magnitude alone. Canon assigns enum state (C1_CRITICAL_PATH = ED_HOLD | REDEPLOY | NETWORK_TRANSFER), which up/down cannot express.",
      "type": "object",
      "required": [
        "operation",
        "delay",
        "visible"
      ],
      "oneOf": [
        {
          "properties": {
            "operation": {
              "const": "set_enum"
            },
            "enum_variable": {
              "type": "string"
            },
            "enum_value": {
              "type": "string"
            },
            "delay": {
              "$ref": "#/$defs/delay"
            },
            "visible": {
              "type": "boolean"
            }
          },
          "required": [
            "operation",
            "enum_variable",
            "enum_value",
            "delay",
            "visible"
          ],
          "additionalProperties": false
        },
        {
          "properties": {
            "operation": {
              "enum": [
                "increment",
                "decrement"
              ]
            },
            "variable": {
              "$ref": "#/$defs/seasonVariable"
            },
            "magnitude": {
              "enum": [
                "slight",
                "moderate",
                "major"
              ]
            },
            "delay": {
              "$ref": "#/$defs/delay"
            },
            "visible": {
              "type": "boolean"
            }
          },
          "required": [
            "operation",
            "variable",
            "magnitude",
            "delay",
            "visible"
          ],
          "additionalProperties": false
        },
        {
          "properties": {
            "operation": {
              "const": "move_band"
            },
            "variable": {
              "$ref": "#/$defs/seasonVariable"
            },
            "to_band": {
              "$ref": "#/$defs/band"
            },
            "delay": {
              "$ref": "#/$defs/delay"
            },
            "visible": {
              "type": "boolean"
            }
          },
          "required": [
            "operation",
            "variable",
            "to_band",
            "delay",
            "visible"
          ],
          "additionalProperties": false
        }
      ]
    },
    "delay": {
      "enum": [
        "immediate",
        "next_chapter",
        "act"
      ]
    },
    "capability_block_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "block_id",
        "block_version",
        "catalogue_version"
      ],
      "description": "R3 B7. THE ONE DEFINITION. A reference to a catalogue capability block, so a scene decision binds to real facility capability rather than to prose.\n\nNOTE WHAT IS NOT IDENTITY: tool_id. R3-tasks.md proposed {tool_id, block_id, catalogue_version}, and that CONTRADICTS the shared-credit design -- capability-block.schema.json states that two items in two different tools referencing ONE block share credit. Binding a ref to a tool would give the same block two different refs depending on which tool you arrived through, and a facility doing one piece of work would be credited twice or not at all. The block is keyed (id, version); the ref is keyed the same way.",
      "properties": {
        "block_id": {
          "type": "string",
          "pattern": "^cb-[a-z0-9-]+$"
        },
        "block_version": {
          "type": "string"
        },
        "catalogue_version": {
          "type": "string",
          "description": "Which catalogue version the ref was resolved against."
        },
        "tool_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "CONTEXT ONLY — where the player encountered this block. NOT part of the reference's identity.\n\nTwo specifications (R3 B7 and scene-and-quest-schema.md 6a) put tool_id INSIDE the ref. Taken as identity that breaks shared credit: a capability block is deliberately shared across tools, so the same block reached through two tools would carry two identities and a facility doing one piece of work would be credited twice or not at all.\n\nBoth readings are reconciled here: the field exists, because a surface needs to know which tool to open, and it is excluded from identity, because credit attaches to the block. Anything comparing or deduplicating refs uses block_id + block_version + catalogue_version."
        }
      }
    }
  },
  "allOf": [
    {
      "description": "A staged scene MUST carry its immediate effect. `staging.post_commit` is required to contain `immediate_effect`; without this, a scene could name the response beat in its staging and hold nothing to put in it — a phase that renders empty, which reads to a player as a choice that did nothing.",
      "if": {
        "required": [
          "staging"
        ],
        "properties": {
          "staging": {
            "type": "object"
          }
        }
      },
      "then": {
        "required": [
          "immediate_effect"
        ],
        "properties": {
          "immediate_effect": {
            "type": "object"
          }
        }
      }
    }
  ]
}
