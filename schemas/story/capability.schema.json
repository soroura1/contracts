{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://citadel.contracts/schemas/story/capability.schema.json",
  "title": "Capability",
  "description": "SG-1 C3. AN OPERATIONAL CAPABILITY THE PARTICIPANT CAN COMMIT — and the reason opportunity in this product is not a resource bar.\n\n★ A CAPABILITY IS A NAMED HOLDER IN A NAMED STATE. Never a pool, a meter, a budget, a point, a token or a countdown. Canon names time, trust, workload, service capacity and evidence as the currencies of this world and sets no prices for any of them; a number invites optimisation, which is the behaviour this product exists to make harder rather than easier.\n\nSo there is no `amount` here, and no `remaining`, and adding one is a change to what the product is. What a capability has instead is a HOLDER — a person, a set of equipment, a route, a record — who can be somewhere, doing something, and who can therefore not be somewhere else. That exclusivity is the whole constraint, and it survives the removal of every clock, which is why the non-timed accessibility path carries the same trade-off rather than a relaxed one.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "name_key",
    "held_by",
    "available_state",
    "how_known",
    "derivedFrom"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^cap\\.",
      "description": "Referenced by a decision option's `commits`. Prefixed so a dangling reference is visible on sight."
    },
    "name_key": {
      "type": "string",
      "minLength": 1,
      "description": "The locale key. A capability the participant meets in one language only is a capability half the audience cannot reason about."
    },
    "held_by": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "id"],
      "description": "WHO OR WHAT HOLDS IT. A capability with no holder is a pool with a different name.",
      "properties": {
        "kind": {
          "enum": ["person", "equipment", "route", "record"]
        },
        "id": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "available_state": {
      "type": "string",
      "minLength": 1,
      "description": "What canon says is available, in canon's own terms — 'the roster safely covers about 28 while 30 are occupied', 'the set exists but must be found, verified and delivered'. Prose, deliberately: a figure copied out of canon into a field the engine can do arithmetic on is a quantity by the back door."
    },
    "how_known": {
      "type": "string",
      "pattern": "^inst\\.",
      "description": "The instrument that reports its status. ★ A capability the participant cannot READ is a hidden variable, and a hidden variable that closes an option is the definition of an arbitrary game."
    },
    "consumed_by": {
      "type": "array",
      "default": [],
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "What commits it, in the world's terms. Not an exhaustive engine list — the authoritative binding is the option's own `commits`."
    },
    "opens": {
      "type": "array",
      "default": [],
      "items": { "type": "string", "minLength": 1 }
    },
    "closes": {
      "type": "array",
      "default": [],
      "items": { "type": "string", "minLength": 1 },
      "description": "What becomes unavailable once it is committed. This is where the trade-off lives, and it is stated in words because canon states it in words."
    },
    "residue": {
      "type": ["string", "null"],
      "default": null,
      "description": "What remains after the incident closes. Null only where canon is silent — and where canon is silent, `unresolved` on the scene is the honest place for it."
    },
    "derivedFrom": {
      "type": "string",
      "minLength": 1,
      "description": "The canon passage. Required, so a reviewer can see what is transcription and what is interpretation."
    }
  }
}
